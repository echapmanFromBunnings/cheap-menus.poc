---
layout: default
title: "The Gelato & Icecream Factory - Menu Image 1"
---

<style>
  body {
    margin: 0;
    padding: 0;
    overflow: hidden;
    background: #000;
  }
  
  .fullscreen-container {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #000;
  }
  
  .fullscreen-image {
    width: 100%;
    height: 100%;
    object-fit: contain;
    transition: transform 0.3s ease;
  }
  
  .nav-overlay {
    position: fixed;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    background: rgba(0, 0, 0, 0.7);
    padding: 15px 25px;
    border-radius: 10px;
    z-index: 1000;
    transition: opacity 0.3s ease;
  }
  
  .nav-overlay.hidden {
    opacity: 0;
    pointer-events: none;
  }
  
  .nav-overlay a, .nav-overlay button {
    color: white;
    text-decoration: none;
    margin: 0 10px;
    font-weight: bold;
    padding: 8px 15px;
    background: #C85A7A;
    border-radius: 5px;
    display: inline-block;
    font-size: 14px;
    border: none;
    cursor: pointer;
    font-family: inherit;
  }
  
  .nav-overlay a:hover, .nav-overlay button:hover {
    background: #D4A574;
  }
  
  .toggle-buttons-btn {
    position: fixed;
    top: 20px;
    right: 20px;
    background: rgba(200, 90, 122, 0.8);
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
    font-weight: bold;
    z-index: 1001;
    transition: opacity 0.3s ease;
  }
  
  .toggle-buttons-btn:hover {
    background: rgba(212, 165, 116, 0.8);
  }
  
  .toggle-buttons-btn.hidden {
    opacity: 0;
    pointer-events: none;
  }
  
  .timer-display {
    position: fixed;
    top: 20px;
    left: 20px;
    background: rgba(200, 90, 122, 0.8);
    color: white;
    padding: 10px 20px;
    border-radius: 5px;
    font-weight: bold;
    font-size: 18px;
    z-index: 1001;
    font-family: 'Courier New', monospace;
  }
  
  .timer-display.hidden {
    display: none;
  }
</style>

<div class="fullscreen-container">
  <img src="designcrowd_7d0z85hb6vzvvcvhvn3ptbmzqe_image.jpg" alt="Menu Image 1" class="fullscreen-image" id="menuImage">
</div>

<button class="toggle-buttons-btn" id="toggleButtonsBtn">Show/Hide Buttons</button>

<div class="timer-display" id="timerDisplay">Next: 0s</div>

<div class="nav-overlay" id="navOverlay">
  <a href="index.html">Home</a>
  <a href="5.html">Page 5</a>
  <a href="6.html">Page 6</a>
  <a href="7.html">Page 7</a>
  <a href="8.html">Page 8</a>
  <button id="rotateBtn">Rotate 90°</button>
</div>

<script>
  // Parse URL parameters
  const urlParams = new URLSearchParams(window.location.search);
  const kioskMode = urlParams.get('kiosk') === 'true';
  const rotationParam = urlParams.get('rotation');
  const transitionParam = urlParams.get('transition');
  
  // Current page number (5-8)
  const currentPage = 5;
  const pages = [5, 6, 7, 8];
  
  // Parse or generate transition times
  let transitionTimes = [];
  if (transitionParam) {
    transitionTimes = transitionParam.split(',').map(t => parseInt(t.trim(), 10)).filter(t => !isNaN(t) && t > 0);
  }
  
  // If no valid transition times, generate random values (15-30 seconds)
  if (transitionTimes.length === 0) {
    const numTransitions = pages.length;
    for (let i = 0; i < numTransitions; i++) {
      transitionTimes.push(Math.floor(Math.random() * 16) + 15); // 15-30 seconds
    }
    
    // Update URL with generated transition times
    const newUrl = new URL(window.location.href);
    newUrl.searchParams.set('transition', transitionTimes.join(','));
    window.history.replaceState({}, '', newUrl.toString());
  }
  
  // Rotation state
  let rotation = rotationParam ? parseInt(rotationParam, 10) : 0;
  
  // Get elements
  const menuImage = document.getElementById('menuImage');
  const navOverlay = document.getElementById('navOverlay');
  const toggleButtonsBtn = document.getElementById('toggleButtonsBtn');
  const rotateBtn = document.getElementById('rotateBtn');
  const timerDisplay = document.getElementById('timerDisplay');
  
  // Kiosk mode: hide buttons by default
  if (kioskMode) {
    navOverlay.classList.add('hidden');
    timerDisplay.classList.add('hidden');
  }
  
  // Apply initial rotation if specified
  if (rotation !== 0) {
    menuImage.style.transform = 'rotate(' + rotation + 'deg)';
  }
  
  // Auto-rotation functionality
  let currentIndex = pages.indexOf(currentPage);
  let timeRemaining = transitionTimes[currentIndex];
  let timerInterval;
  
  function updateTimer() {
    if (!kioskMode) {
      timerDisplay.textContent = 'Next: ' + timeRemaining + 's';
    }
  }
  
  function buildUrl(pageNum) {
    const url = new URL(window.location.origin + '/' + pageNum + '.html');
    if (kioskMode) {
      url.searchParams.set('kiosk', 'true');
    }
    if (rotation !== 0) {
      url.searchParams.set('rotation', rotation.toString());
    }
    url.searchParams.set('transition', transitionTimes.join(','));
    return url.toString();
  }
  
  function startAutoRotation() {
    updateTimer();
    
    timerInterval = setInterval(function() {
      timeRemaining--;
      updateTimer();
      
      if (timeRemaining <= 0) {
        // Move to next page
        currentIndex = (currentIndex + 1) % pages.length;
        const nextPage = pages[currentIndex];
        window.location.href = buildUrl(nextPage);
      }
    }, 1000);
  }
  
  // Start auto-rotation
  startAutoRotation();
  
  // Rotate button handler
  rotateBtn.addEventListener('click', function() {
    rotation = (rotation + 90) % 360;
    menuImage.style.transform = 'rotate(' + rotation + 'deg)';
  });
  
  // Toggle buttons visibility
  toggleButtonsBtn.addEventListener('click', function() {
    navOverlay.classList.toggle('hidden');
    if (navOverlay.classList.contains('hidden')) {
      toggleButtonsBtn.textContent = 'Show Buttons';
    } else {
      toggleButtonsBtn.textContent = 'Hide Buttons';
    }
  });
  
  // Update toggle button text based on initial state
  if (navOverlay.classList.contains('hidden')) {
    toggleButtonsBtn.textContent = 'Show Buttons';
  } else {
    toggleButtonsBtn.textContent = 'Hide Buttons';
  }
  
  // Update navigation links to preserve URL parameters
  const navLinks = navOverlay.querySelectorAll('a[href^="5.html"], a[href^="6.html"], a[href^="7.html"], a[href^="8.html"]');
  navLinks.forEach(function(link) {
    const pageMatch = link.href.match(/(\d+)\.html/);
    if (pageMatch) {
      link.href = buildUrl(parseInt(pageMatch[1]));
    }
  });
</script>
